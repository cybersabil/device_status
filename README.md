# Employee Device Dashboard Frontend

Public static frontend for GitHub Pages. No real employee data or secrets belong in this repository.

The authenticated **Master Data** screen validates and updates the employee list through the protected backend. It never stores the master in the public frontend repository.